{% autoescape off %}Hello,

Someone (hopefully you) asked to reset the password for {{ user.email|default:user.username }}.

Choose a new password here:
{% if link %}{{ link }}{% else %}{{ protocol }}://{{ domain }}{% url 'password_reset_confirm' uidb64=uid token=token %}{% endif %}

The link works once and expires in 3 days. If you didn't ask for this, you can ignore this email; your password won't change.

{{ company|default:"PRETTY EVENTS LTD." }}
{% endautoescape %}
